#include<bits/stdc++.h>
using namespace std;

/*Spaning tree -- a tree which has N nodes & n-1 edges with every node is reachable by other node*/
/*Minimum spanning tree -- sum of all edges weights in the tree is minimum */
/*A Graph can have a more than one spanning tree*/

int main()
{
    
    return 0;
}