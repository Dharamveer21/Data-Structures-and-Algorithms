#include <bits/stdc++.h>
using namespace std;

// max array size global declare is 10^7
const int n = 1e7 + 10;// takes memory in data segment with has no limit as such
int arr[n];

int main()
{
    int m = 1e5 + 10;
    // max array size local declare is 10^5
    int a[m];// takes memeory in stack which has a limited memory and shows overflow

    return 0;
}