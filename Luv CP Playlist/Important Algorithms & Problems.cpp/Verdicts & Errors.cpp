#include <bits/stdc++.h>
using namespace std;

int main()
{
    // compilation error - syntax error
    // memory limit exceed - declared an very large array
    /*
       runtime error or segmentation fault -
       access ileegal memeory address like a[n] where n is size of array
       access negative index
    */

    // Time limit exceeded - this only comes in execution time but not compilation time

    // tle errors in recurrsion due to infinite time , runtime error due to stack overflow 

    // 10^7 interations in 1 sec - time complexity

    return 0;
}